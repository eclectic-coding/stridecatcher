<%= debug(@activity.inspect) %>
<p>Duration:
  <span><%= format_duration(@activity.duration) if @activity.duration.present? %></span>
</p>
<p>Pace:
  <span><%= format_duration(@activity.calculated_pace) if @activity.calculated_pace.present? %></span>
</p>